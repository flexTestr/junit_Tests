<?xml version="1.0" encoding="UTF-8"?>
<project name="WorkflowTests" default="test" basedir=".">

    <property name="src.dir" value="."/>
    <property name="build.dir" value="build"/>
    <property name="lib.dir" value="lib"/>
    <property name="report.dir" value="${build.dir}/reports"/>

    <path id="classpath">
        <fileset dir="${lib.dir}" includes="*.jar"/>
        <pathelement location="${build.dir}/classes"/>
    </path>

    <target name="init">
        <mkdir dir="${build.dir}/classes"/>
        <mkdir dir="${report.dir}"/>
    </target>

    <target name="compile" depends="init">
        <javac srcdir="${src.dir}" destdir="${build.dir}/classes" includeantruntime="false">
            <classpath refid="classpath"/>
        </javac>
    </target>

    <target name="test" depends="compile">
        <junit printsummary="yes" haltonfailure="no" fork="yes">
            <classpath refid="classpath"/>
            <formatter type="plain"/>
            <formatter type="xml"/>
            <batchtest fork="yes" todir="${report.dir}">
                <fileset dir="${src.dir}">
                    <include name="**/*Test.java"/>
                </fileset>
            </batchtest>
        </junit>
         <junitreport todir=".">
        <fileset dir="." includes="TEST-*.xml"/>
    </junitreport>

    <copy file="TESTS-TestSuites.xml" tofile="results.xml"/>
    </target>

    <target name="main" depends="test">
        <echo message="Build complete."/>
    </target>

</project>
